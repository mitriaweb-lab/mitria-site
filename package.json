{
  "name": "mitria-site",
  "version": "1.0.0",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "optimize": "node optimize-images.js",
    "build": "node optimize-images.js && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  },
  "dependencies": {
    "sharp": "^0.33.2"
  }
}
